# Normalise to LF in the repo. Windows CRLF leaking into shell scripts breaks
# builds on macOS in ways that are painful to diagnose remotely. See brief §6.
* text=auto eol=lf

*.png binary
*.jpg binary
*.pdf binary
*.db binary
*.pb binary
